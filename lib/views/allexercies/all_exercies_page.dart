import 'package:flutter/material.dart';

class AllExerciesPage extends StatelessWidget {
  const AllExerciesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold());
  }
}