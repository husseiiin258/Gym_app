import 'package:flutter/material.dart';

class BackExercises extends StatelessWidget {
  const BackExercises({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
